- name: Leave swarm for a node
  docker_swarm:
    state: absent
    force: true
- name: "Add nodes"
  docker_swarm:
    state: join
    advertise_addr: workers
    join_token: "{{ token }}"
    remote_addrs: [ '35.246.68.219:2377' ]